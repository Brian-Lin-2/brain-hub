# JFrog

JFrog (Artifactory) is a Binary Repository Manager. While tools like GitHub manage your source code, JFrog manages the artifacts created from that code (compiled binaries, Docker images, Python packages, libraries, and ZIP files that are actually deployed to servers). Essentially it stores binaries that are used for deployment. It acts as the `Source of Truth`.
