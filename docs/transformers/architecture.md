# Architecture

A transformer is pretty straight forward. It is just a bunch of attention and feed forward layers with positional encoding.

![Transformer architecture](/docs/assets/git/transformer.png)

## Overview

Inside a transformer, it is typically just multiple layers of MHA and FFNN until an output is generated.

`Multi-Headed Attention Layer (MHA)` - Essentially parallel processing for attention. Each head will calculate their own attention and at the end the results are combined.
`Feed Forward Neural Network (FFNN)` - This is just a basic neural network. It is actually what progresses the model from the input to output text.

## Encoder

An Encoder uses `unmasked self-attention` making it bidirectional. This means every token can see every other token. This gives a form of "global context" to the Encoder. This makes Encoders good for text analysis.

## Decoder

A Decoder uses `masked self-attention` making it unidirectional. This means when generating text, a model cannot see the next words. This makes Decoders good for text generation.

## Encoder-Decoder

The traditional transformer has an encoder-decoder architecture. Basically the encoder is used to convert the sentence into a meaningful embedding and the decoder uses that embedding to generate an output. The decoder uses `cross-attention` to examine the output generated by the Encoder.

Both is needed because the Encoder helps us get the global context of the prompt. This makes sure the transformer doesn't hallucinate. However, as you know text is generated one token at a time which is where the Decoder comes in. It'll take in the embeddings and use the learned context to generate an output.

## Additional

### Positional Embedding

This helps assign positional weights to each token. This matters a lot for sentences as the position of words can greatly change the meaning

### LayerNorm

Layer Normalization is an additional technique done after each MHA and FFNN layers in the transformer. To help stabilize and accelerate the training of neural networks.

It basically just normalizes all the neuron activations into a consistent range, ensuring the network remains stable.

**Common Types:**

`Pre-Norm` - This is the defacto standard nowadays. This normalization happens before the data enters the main sub-layer (ie. `MHA` or `FFNN`). This makes the neural network very stable during training
`Post-Norm` - This was the original method where normalization happens after the data exits the main sub-layer (ie. `MHA` or `FFNN`). However, this isn't used nowadays because it is very difficult to train.
`RMS-Norm` - Stands for Root Mean Square Layer Normalization which is a simplified version of Layer Normalization. Designed to have the same stabilization benefits, but with less computational overhead.

> Nowadays, Pre-Norm + RMS-Norm are used.

## Context Window

Essentially the model's short-term memory. It represents the maximum amount of information, measured in tokens, that the LLM can process and consider at any single moment.

In a conversation with an LLM, it actually stores your entire chat history with it and passes the previous history + your input on every new input. Once the context window gets too big, the LLM will need to either compress the tokens or drop the earlier ones.

"Context Rot" is a phenomenon that occurs where in long-running AI sessions, the quality, accuracy, and coherence of the model's response slowly decays over time.

## In-Context Learning

ICL is a concept where the LLM is able to learn on the spot, even if it hasn't been trained on that specific data yet.

`Zero-Shot Learning` - This is where the question is asked without examples. Performance heavily depends on performance of initial model
`Few-Shot Learning` - This is where the prompt contains examples of input / output. This lets the model learn and produce better performance
